{
  "name": "a",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": " nodemon server.js",
    "test": "concurrently \"nodemon server.js\" \"npm run client\"",
    "installall": "concurrently \"npm run serverInstall\" \"npm run clientInstall\"",
    "server": "node server.js",
    "client": "cd Client && npm start",
    "serverInstall":"npm i",
    "clientInstall":"cd Client && npm i",
    "importwindows":"cmd /c \"mysql -u root -p test < sql.sql\"",
    "build":"cd Client && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mysql": "^2.18.1",
    "nodemon": "^2.0.20"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
